# Python Version Requirements:
# - Python 3.11 (recommended for GPU support with RAPIDS cuML)
# - Python 3.10-3.12 also supported
# - Python 3.13 may have compatibility issues with RAPIDS cuML

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# CPU HDBSCAN (fallback)
hdbscan>=0.8.27

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0
mplfinance>=0.12.10b0

# Utilities
joblib>=1.0.0
tqdm>=4.62.0

# Optional GPU dependencies for CUDA 12.x (install separately)
# pip install cupy-cuda12x>=13.0.0 cuml-cu12>=25.10.0 --extra-index-url=https://pypi.nvidia.com
# Note: Requires CUDA 12.x compatible GPU and drivers
